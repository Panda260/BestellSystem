version: "3.9"

services:
  bestellsystem:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      BESTELL_PASSWORD: "changeme"
      SESSION_SECRET: "replace-me"
    volumes:
      - ./data:/app/data
      - .:/app
      - /app/node_modules
